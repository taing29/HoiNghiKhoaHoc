
@{
    ViewData["Title"] = "";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
<link rel="stylesheet" href="~/css/style.css" /> 
<div class="d-flex">
    <!-- Sidebar -->
    <div class="sidebar col-md-2">
        <h4 class="px-3 text-dark">Quản trị</h4>
        <a class="active" href="#">Bảng điều khiển</a>
        <a href="@Url.Action("Index", "Conferences", new { area = "Admin" })">Quản lý hội nghị</a>
        <a href="@Url.Action("Index", "Users", new { area = "Admin" })">Quản lý người dùng</a>
        <a href="@Url.Action("Index", "Categories", new { area = "Admin" })">Quản lý danh mục</a>
        <a href="@Url.Action("Index", "Speakers", new { area = "Admin" })">Quản lý diễn giả</a>
        <a href="@Url.Action("Index", "Dashboard", new { area = "Admin" })">Thống kê</a>
    </div>

    <!-- Content -->
    <div class="main-content col-md-10">
        <header class="d-flex justify-content-between mb-4">
            <h2>@ViewData["Title"]</h2>
            <div>
                <span class="me-3"><i class="fas fa-user-circle"></i>Admin</span>
                <a href="@Url.Action("Login", "Account", new { area = "Identity" })">Đăng xuất</a>
            </div>
        </header>
        <main>
            @RenderBody()
        </main>
        @RenderSection("Scripts", required: false)
    </div>
</div>

<style>
    body {
        background-color: #f4f4f9;
    }

    .sidebar {
        height: 100vh;
        /* background-color: white; */
        color: white;
        padding-top: 1rem;
    }

        .sidebar a {
            color: black;
            text-decoration: none;
            display: block;
            padding: 12px 20px;
        }

            .sidebar a:hover {
                background-color: #0033CC;
            }

        .sidebar .active {
            /* background-color: #3366CC; */
            font-weight: bold;
        }

    .main-content {
        padding: 30px;
    }

    .card-box {
        border-radius: 15px;
        padding: 20px;
        color: white;
    }

    .bg-gradient-orange {
        background: linear-gradient(to right, #fda085, #f6d365);
    }

    .bg-gradient-blue {
        background: linear-gradient(to right, #4facfe, #00f2fe);
    }

    .bg-gradient-green {
        background: linear-gradient(to right, #43e97b, #38f9d7);
    }
</style>
